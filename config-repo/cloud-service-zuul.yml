server:
  port: 8083
  
hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 40000

ribbon:
  ReadTimeout: 10000
  ConnectTimeout: 10000

zuul:
  routes:
    api-a:
      path: /api-a/**
      serviceId: CLOUD-EXAMPLE-API1
